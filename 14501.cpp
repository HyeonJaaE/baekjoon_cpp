#include<bits/stdc++.h>
using namespace std;
using pi = pair<int, int>;

DP[]
vector<pi> v;
int N;

int main(){
    cin >> N;
    for(int i = 0 ; i < N ; i++){
        int T, P; cin >> T >> P;
        v.push_back(pi(T,P));
    }
}