#include <bits/stdc++.h>
using namespace std; 

int T;
int N, M;

void init(){
    cin >> N >> M;
}

void solve(){
    for(int i = 0 ; i < ){
        
    }
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> T;
    while(T--){
        init();
        solve();
    }
}