#include<bits/stdc++.h>
using namespace std;

const int MAXN = 1000000;

int arr[MAXN];
int N;

void init(){
    cin >> N;
    for(int i = 0 ; i < N ; i++)
        cin >> arr[i];
}

void solve(){
    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    init();
    solve();
}